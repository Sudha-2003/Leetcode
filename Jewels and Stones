class Solution:
    def numJewelsInStones(self, jewels: str, stones: str) -> int:
        a=[]
        for i in range(len(stones)):
            if stones[i] in jewels:
                a.append(i)
        return len(a)
        
